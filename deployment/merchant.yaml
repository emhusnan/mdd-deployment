apiVersion: apps/v1
kind: Deployment
metadata:
  name: merchant-deployment-dev
  namespace: dev
  labels:
    app: merchant-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: merchant-dev
  template:
    metadata:
      labels:
        app: merchant-dev
    spec:
      containers:
        - name: merchant-dev
          image: emhusnan/mdd-merchant-service:v0.0.3-dev
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: merchant-service-dev
  namespace: dev
spec:
  selector:
    app: merchant-dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: merchant-ingress-dev
  namespace: dev
  annotations:
    plugins.konghq.com: cors
spec:
  rules:
    - host: api-dev.mdd.co.id
      http:
        paths:
          - path: /merchant-service
            backend:
              serviceName: merchant-service-dev
              servicePort: 80
---
apiVersion: configuration.konghq.com/v1
kind: KongIngress
metadata:
  name: merchant-ingress-dev
  namespace: dev
route:
  strip_path: true

