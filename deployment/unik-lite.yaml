apiVersion: apps/v1
kind: Deployment
metadata:
  name: unik-lite-deployment-dev
  namespace: dev
  labels:
    app: unik-lite-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: unik-lite-dev
  template:
    metadata:
      labels:
        app: unik-lite-dev
    spec:
      containers:
        - name: unik-lite-dev
          image: emhusnan/mdd-unik-lite-service:v0.0.1-dev
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: unik-lite-service-dev
  namespace: dev
spec:
  selector:
    app: unik-lite-dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: unik-lite-ingress-dev
  namespace: dev
  annotations:
    plugins.konghq.com: cors
spec:
  rules:
    - host: api-dev.mdd.co.id
      http:
        paths:
          - path: /unik-lite-service
            backend:
              serviceName: unik-lite-service-dev
              servicePort: 80

---
apiVersion: configuration.konghq.com/v1
kind: KongIngress
metadata:
  name: unik-lite-ingress-dev
  namespace: dev
route:
  strip_path: true

