apiVersion: apps/v1
kind: Deployment
metadata:
  name: hk-deployment-dev
  namespace: dev
  labels:
    app: hk-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hk-dev
  template:
    metadata:
      labels:
        app: hk-dev
    spec:
      containers:
        - name: hk-dev
          image: emhusnan/mdd-hk-service:v0.0.5-dev
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: hk-service-dev
  namespace: dev
spec:
  selector:
    app: hk-dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: hk-ingress-dev
  namespace: dev
  annotations:
    plugins.konghq.com: cors
spec:
  rules:
    - host: api-dev.mdd.co.id
      http:
        paths:
          - path: /hk-service
            backend:
              serviceName: hk-service-dev
              servicePort: 80

---
apiVersion: configuration.konghq.com/v1
kind: KongIngress
metadata:
  name: hk-ingress-dev
  namespace: dev
route:
  strip_path: true

